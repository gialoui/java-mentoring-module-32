server:
  port: 8762

spring:
  main:
    web-application-type: reactive
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: one-service
          uri: lb://one-service
          predicates:
            - Path=/one/**
        - id: two-service
          uri: lb://two-service
          predicates:
            - Path=/two/**

eureka:
  client:
    registerWithEureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka